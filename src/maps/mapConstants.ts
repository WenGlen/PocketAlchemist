/**
 * 地圖相關常數
 * 集中管理地圖邊界、視埠、初始設定等數值
 */

// ────────────────────────────────────────────────────────────────
// 地圖預設值
// ────────────────────────────────────────────────────────────────

/** 地圖邊界預設寬度 (當地圖資料未定義時使用) */
export const DEFAULT_MAP_WIDTH = 1600;

/** 地圖邊界預設高度 (當地圖資料未定義時使用) */
export const DEFAULT_MAP_HEIGHT = 1200;

// ────────────────────────────────────────────────────────────────
// 視埠
// ────────────────────────────────────────────────────────────────

/** 視埠初始尺寸 (ResizeObserver 尚未取得實際尺寸前的 fallback) */
export const DEFAULT_VIEWPORT_SIZE: { w: number; h: number } = {
  w: 400,
  h: 300,
};

// ────────────────────────────────────────────────────────────────
// 時間常數
// ────────────────────────────────────────────────────────────────

/** Proximity 檢測節流間隔 (ms) */
export const PROXIMITY_TICK_MS = 100;

// ────────────────────────────────────────────────────────────────
// 初始設定
// ────────────────────────────────────────────────────────────────

/** 預設初始地圖 ID */
export const DEFAULT_MAP_ID = 'MAP-field-001';

/** 預設初始任務 ID */
export const DEFAULT_QUEST_ID = 'QST-main-001';
