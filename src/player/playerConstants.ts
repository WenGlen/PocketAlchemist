/**
 * 玩家相關常數
 * 集中管理玩家移動、碰撞、血量、視覺尺寸等數值
 */

// ────────────────────────────────────────────────────────────────
// 移動與碰撞
// ────────────────────────────────────────────────────────────────

/** 玩家移動速度 (px/s) */
export const PLAYER_SPEED = 200;

/** 玩家碰撞半徑 (px) */
export const PLAYER_RADIUS = 20;

/** 玩家預設出生點座標 (當地圖未定義 spawnPoint 時使用) */
export const DEFAULT_SPAWN = { x: 400, y: 300 };

// ────────────────────────────────────────────────────────────────
// 血量
// ────────────────────────────────────────────────────────────────

/** 血量上限 */
export const HP_MAX = 100;

// ────────────────────────────────────────────────────────────────
// 視覺尺寸
// ────────────────────────────────────────────────────────────────

/** 玩家視覺預設尺寸 */
export const PLAYER_VISUAL_SIZE = {
  width: 40,
  height: 40,
} as const;
