/**
 * 物件視覺與動畫常數
 * 集中管理等角視覺比例、透明度、UI 間距、動畫時序等數值
 */

// ════════════════════════════════════════════════════════════════
// 等角視覺 (Isometric Visual)
// ════════════════════════════════════════════════════════════════

/**
 * 等角視覺常數
 * 用於物件的 hitTest 判定與視覺呈現
 */
export const ISO_VISUAL = {
  /** hitTest 圓心 Y 偏移係數 (相對於物件高度，向下偏移) */
  HIT_CENTER_Y_OFFSET: 0.25,

  /** hitTest 半徑縮放比例 (相對於物件寬高最小值) */
  HIT_RADIUS_SCALE: 0.75,

  /** 定位環高度比例 (相對於物件寬度) */
  RING_HEIGHT_RATIO: 0.3,

  /** 漣漪動畫尺寸比例 (相對於物件寬度) */
  RIPPLE_SIZE_RATIO: 0.5,
} as const;

// ════════════════════════════════════════════════════════════════
// 預設尺寸
// ════════════════════════════════════════════════════════════════

/** 實體預設半徑 (NPC、資源點等無 radius 定義時的 fallback) */
export const DEFAULT_ENTITY_RADIUS = 24;

/** 定位環預設邊框寬度 (px) */
export const RING_BORDER_WIDTH = 2;

// ════════════════════════════════════════════════════════════════
// Debug 視覺
// ════════════════════════════════════════════════════════════════

/**
 * Debug hitbox 視覺常數
 */
export const DEBUG_VISUAL = {
  /** hitbox 外框虛線寬度 (px) */
  OUTLINE_WIDTH: 1.5,

  /** 圓心標記尺寸 (px) */
  MARKER_SIZE: 6,
} as const;

// ════════════════════════════════════════════════════════════════
// 透明度 (Opacity)
// ════════════════════════════════════════════════════════════════

/**
 * 物件狀態透明度
 * 用於表示不同互動狀態下的視覺區分
 */
export const OPACITY = {
  /** 在互動範圍內 */
  IN_RANGE: 1,

  /** 超出互動範圍 (略微淡化) */
  OUT_OF_RANGE: 0.8,

  /** 已禁用 / 已採完 */
  DISABLED: 0.65,

  /** 暈眩狀態 (怪物) */
  STUNNED: 0.6,

  /** Debug hitbox 顯示 */
  DEBUG_HITBOX: 0.75,
} as const;

// ════════════════════════════════════════════════════════════════
// UI 間距
// ════════════════════════════════════════════════════════════════

/**
 * 泡泡提示 UI 間距
 */
export const BUBBLE_SPACING = {
  /** 上邊距 */
  marginTop: 12,

  /** 下邊距 */
  marginBottom: 4,
} as const;

/**
 * 浮動文字偏移量
 */
export const FLOAT_TEXT_OFFSET = {
  /** X 軸偏移 (向左) */
  x: 28,

  /** Y 軸偏移 (向上，相對於物件頂部) */
  y: 50,
} as const;

// ════════════════════════════════════════════════════════════════
// 動畫時序
// ════════════════════════════════════════════════════════════════

// ── UI 回饋動畫 ──────────────────────────────────────────────────

/** 放置道具回饋動畫時間 (ms) */
export const PLACE_FEEDBACK_MS = 180;

/** 任務完成彈窗顯示時間 (ms) */
export const QUEST_CELEBRATION_MS = 2200;

/** 合成完成後清空合成槽延遲 (ms) */
export const CRAFT_CLEAR_DELAY_MS = 450;

// ── 互動回饋清除 ─────────────────────────────────────────────────

/** 資源採集浮動文字清除延遲 (ms) */
export const FEEDBACK_CLEAR_MS = 700;

/** 暈眩提示浮動文字清除延遲 (ms) */
export const STUN_FEEDBACK_CLEAR_MS = 700;

// ── CSS 動畫時間 (用於 inline style) ─────────────────────────────

/** 怪物暈眩恢復動畫時間 */
export const MONSTER_STUN_RECOVER_DURATION = '0.5s';
