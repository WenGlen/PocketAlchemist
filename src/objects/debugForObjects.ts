/**
 * 地圖物件開發用調試設定。
 * 正式出版前應確保所有旗標為 false。
 */
export const debugConfig = {
  /**
   * 顯示所有地圖物件的 Hitbox 邊界（紅色虛線），
   * 以及資源點的採集互動範圍（橘色虛線圓）、
   * 怪物的攻擊判定範圍（紫色虛線圓）。
   * 預設 false；開啟時請勿提交至正式分支。
   */
  showHitbox: true,
};
